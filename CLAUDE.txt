 ✅ 三项需求全部完成

  1. 🎨 多主题换肤系统

  默认主题已从粉色改为「蜂蜜柠檬糖」（暖橘色），草莓糖为可选方案。

  ┌───────────────────────┬──────────────┬──────────┐
  │         主题          │    主色调    │   风格   │
  ├───────────────────────┼──────────────┼──────────┤
  │ 🍯 蜂蜜柠檬糖（默认） │ #FF8C42 暖橘 │ 温暖活泼 │
  ├───────────────────────┼──────────────┼──────────┤
  │ 🍃 薄荷糖             │ #00BFA5 青绿 │ 清新自然 │
  ├───────────────────────┼──────────────┼──────────┤
  │ 🍓 草莓糖             │ #FF6B9D 粉色 │ 甜美可爱 │
  ├───────────────────────┼──────────────┼──────────┤
  │ 🫐  蓝莓糖             │ #667EEA 蓝紫 │ 沉静优雅 │
  └───────────────────────┴──────────────┴──────────┘

  实现方式：
  - 所有颜色 CSS 变量化（--c-primary, --c-bg 等 22 个变量）
  - Tailwind 自定义 candy-* 颜色类（text-candy-primary, bg-candy-bg 等）
  - 主题切换实时生效，无需刷新
  - 偏好保存到 localStorage

  切换入口： 主页底部新增「🎨 配色方案」区域，点击色块即可切换。

  ---
  2. 🖥️ 本地测试入口
  - 偏好保存到 localStorage

  切换入口： 主页底部新增「🎨 配色方案」区域，点击色块即可切换。

  ---
  2. 🖥️ 本地测试入口

  新增了不依赖 pnpm/coze 的本地运行方式：

  # 安装依赖（首次）
  npm install --ignore-scripts
  # 启动开发服务器（带热更新）
  npm run dev:local
  # → 浏览器打开 http://localhost:5000

  # 仅构建
  npm run build:local

  # 预览构建产物
  npm run preview

  ---
  3. 📋 题库生成工具

  文件： tools/quiz-generator.html（双击即可在浏览器打开）

  支持的 CSV 格式（从 Excel 直接复制粘贴）：

  ┌──────────┬────────────────────────────────────────────────────┬──────────────────────────────────────────────────────┐
  │   题型   │                      Excel 列                      │                         示例                         │
  ├──────────┼────────────────────────────────────────────────────┼──────────────────────────────────────────────────────┤
  │ 单选题   │ A:题目 B:选项A C:选项B D:选项C E:选项D F:答案(0-3) │ What is...?,Manchester,London,Liverpool,Birmingham,1 │
  ├──────────┼────────────────────────────────────────────────────┼──────────────────────────────────────────────────────┤
  │ 消消乐   │ A:说明 B:左侧词条|右侧词条 C:配对|关系             │ 匹配单词,apple|banana,苹果|香蕉,0-0|1-1              │
  ├──────────┼────────────────────────────────────────────────────┼──────────────────────────────────────────────────────┤
  │ 还原句子 │ A:提示 B:单词|列表 C:正确|顺序                     │ 我喜欢...,I|like|reading,0|1|2                       │
  └──────────┴────────────────────────────────────────────────────┴──────────────────────────────────────────────────────┘

  功能： 解析预览 → 加入题库 → 导出/复制 JSON → 直接导入到应用。

  ---
  📦 构建产物

  dist/index.html              0.53 kB
  dist/assets/index-*.css     17.01 kB (gzip 4.39 kB)
  dist/assets/index-*.js      56.20 kB (gzip 14.79 kB)
  ✓ TypeScript 类型检查: 通过
  ✓ Vite 生产构建: 通过

  项目已准备就绪，可以部署到 GitHub Pages！🎉